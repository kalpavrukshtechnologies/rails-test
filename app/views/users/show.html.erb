<% provide(:title, @user.name) %>
<h1>
  <%= @user.name %>
</h1>
<%= render 'shared/flash_messages' %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
  <%= render 'questions/post_question' %>
  </div>
</div>
<br>
<%= button_to 'Log out', logout_path, method: :delete, class: "btn btn-info btn-lg btn-block" %>
